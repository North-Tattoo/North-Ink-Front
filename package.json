{
  "name": "north-vite",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "node server.js",
    "postinstall": "cd site && npm install && npm run build"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
